{
  "type": "object",
  "description": "Lists the group applications for the given group. Available to administrators of the group or administrators of an organization if the group is part of one. http://resources.arcgis.com/en/help/arcgis-rest-api/index.html#/Group_Applications/02r300000068000000/",
  "title": "groupApplications",
  "properties": {
    "applications": {
      "type": "array",
      "description": "A JSON array of application objects. See Group Application for property information",
      "minLength": 0,
      "items": {
        "$ref": "#/definitions/groupApplication"
      }
    }
  },
  "required": [
    "applications"
  ],
  "definitions": {
    "groupApplication": {
      "type": "object",
      "description": "When an individual user applies to join a group, a group application is created. The group administrators can accept or decline the application. Available only to the group administrators and the administrator of the organization if the group belongs to an organization. http://resources.arcgis.com/en/help/arcgis-rest-api/index.html#/Group_Application/02r30000006r000000/",
      "title": "groupApplication",
      "properties": {
        "username": {
          "type": "string",
          "description": "The username of the user applying to join the group",
          "chance": "first"
        },
        "fullname": {
          "type": "string",
          "description": "The full name of the user applying to join the group",
          "chance": "name"
        },
        "received": {
          "type": "integer",
          "description": "The date the application was received in UNIX time in milliseconds",
          "chance": "timestamp"
        }
      },
      "required": [
        "fullname",
        "received",
        "username"
      ]
    }
  }
}